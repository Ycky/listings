from django.urls import path
from . import views

urlpatterns = [
    # path('category/', views.CategoryView.as_view()),
    # path('image/', views.ImageView.as_view()),
    # path('aam/', views.AAMView.as_view()),
]